import React, { Component } from 'react';

class Title extends Component {
  render() {
    return (
    <h1>This is Aabhas's react app {this.props.title}</h1>
        );
  }
}

export default Title;
